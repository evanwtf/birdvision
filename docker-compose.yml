services:

  # Web UI — upload videos and view results at http://localhost:3587
  web:
    build: .
    image: birdvision:latest
    runtime: nvidia
    environment:
      - NVIDIA_VISIBLE_DEVICES=all
      - NVIDIA_DRIVER_CAPABILITIES=compute,utility
      - GOOGLE_CLIENT_ID=${GOOGLE_CLIENT_ID:-}
      - GOOGLE_CLIENT_SECRET=${GOOGLE_CLIENT_SECRET:-}
      - SESSION_SECRET=${SESSION_SECRET:-}
      - BIRDVISION_DEBUG=${BIRDVISION_DEBUG:-}
    volumes:
      - ./config.yaml:/data/config.yaml:ro
      - ./api_tokens.yaml:/data/api_tokens.yaml:ro
      - ./videos:/data/videos
      - ./results:/data/results
      - ./models:/data/models
    ports:
      - "3587:3587"
    command: ["/usr/local/bin/uv", "run", "scripts/serve.py", "--config", "/data/config.yaml"]
    stop_grace_period: 30s

  # CLI batch runner — drop videos in ./videos/ and run manually
  birdvision:
    build: .
    image: birdvision:latest
    runtime: nvidia
    environment:
      - NVIDIA_VISIBLE_DEVICES=all
      - NVIDIA_DRIVER_CAPABILITIES=compute,utility
    volumes:
      - ./config.yaml:/data/config.yaml:ro
      - ./videos:/data/videos:ro
      - ./results:/data/results
      - ./models:/data/models
    command: ["/usr/local/bin/uv", "run", "scripts/identify_videos.py", "/data/videos", "--config", "/data/config.yaml"]
    stop_grace_period: 30s
    profiles: ["cli"]

  tuner:
    build: .
    image: birdvision:latest
    runtime: nvidia
    environment:
      - NVIDIA_VISIBLE_DEVICES=all
      - NVIDIA_DRIVER_CAPABILITIES=compute,utility
    volumes:
      - ./config.yaml:/data/config.yaml:ro
      - ./videos:/data/videos
      - ./results:/data/results
      - ./models:/data/models
    entrypoint: ["/usr/local/bin/uv", "run", "scripts/tune_single_video.py", "--config", "/data/config.yaml"]
    stop_grace_period: 30s
    profiles: ["tuner"]
