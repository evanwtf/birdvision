# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
dist/
build/

# Models (downloaded automatically, don't commit)
*.pt
*.pth
*.onnx

# HuggingFace / torch cache
.cache/

# Data and results
videos/
results/
output/
models/
train_data/
logs/

# Eval report output (generated, not committed)
eval/report/

# Generated eBird SQLite DB (built from ebird_data/ via import script)
data/ebird_priors.db

# Claude Code local settings
.claude/

# API tokens (real file gitignored; api_tokens.yaml.example is committed)
api_tokens.yaml

# Pi models (compiled HEF/HAR binaries + label map — not committed)
pi/models/*.hef
pi/models/*.har
pi/models/*.onnx
pi/models/species_labels.json

# Pi deps (proprietary Hailo wheels — download from hailo.ai/developer-zone)
pi/deps/*.whl

# Misc
.DS_Store
.env
*.log
